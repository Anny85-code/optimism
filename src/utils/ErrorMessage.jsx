/* eslint-disable */
import React from 'react';
import './ErrorMessage.css';

const ErrorMessage = ({ message }) => (
  <center className="blink-error">{message}</center>
);

export default ErrorMessage;
/* eslint-enable */
